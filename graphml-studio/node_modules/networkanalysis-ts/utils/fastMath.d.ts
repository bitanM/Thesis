/**
 * Calculates `exp(exponent)` using a fast implementation.
 *
 * @param exponent Exponent
 *
 * @return exp(exponent)
 */
export declare function fastExp(exponent: number): number;
/**
 * Calculates `base ^ exponent` using a fast implementation.
 *
 * @param base     Base
 * @param exponent Exponent
 *
 * @return base ^ exponent
 */
export declare function fastPow(base: number, exponent: number): number;
